name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello world
        run: |
          echo "Hello World!"

      - name: Print current date
        run: |
          date

      - name: List directory contents
        run: |
          ls -l
